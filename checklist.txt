ACCENTR - TODO CHECKLIST
========================

SETUP
-----
[ ] 1. Scaffold Next.js app (App Router, TypeScript, Tailwind)
[ ] 2. Install and configure shadcn/ui
[ ] 3. Set up .env.local with keys:
       - DATABASE_URL / DIRECT_URL (Supabase PostgreSQL)
       - AUTH_SECRET
       - ANTHROPIC_API_KEY
       - ELEVENLABS_API_KEY
[ ] 4. Install dependencies:
       - @prisma/client, prisma, @prisma/adapter-pg, pg
       - jose, bcryptjs
       - @anthropic-ai/sdk
       - elevenlabs (or fetch wrapper)

DATABASE
--------
[ ] 5. Write prisma/schema.prisma (PostgreSQL)
       - User (id, email, passwordHash)
       - UserProfile (displayName, avatarUrl, totalXp, streak)
       - Language (userId, code, name)
       - Session (userId, languageId, totalScore, durationSecs, completedAt)
       - Attempt (sessionId, phraseText, audioUrl, score, feedback, phonemes)
       - Achievement (userId, slug)
[ ] 6. Configure prisma.config.ts to load .env.local
[ ] 7. Run prisma migrate dev --name init
[ ] 8. Set up lib/prisma.ts singleton (with @prisma/adapter-pg)

AUTH
----
[ ] 9.  Set up lib/auth.ts — JWT sessions via jose
        - createSession(userId, email)
        - getSession()
        - deleteSession()
[ ] 10. Build signup page + form (app/auth/signup/page.tsx)
        - Server action: hash password, create User + UserProfile, createSession(), redirect
[ ] 11. Build login page + form (app/auth/login/page.tsx)
        - Server action: verify bcrypt hash, createSession(), redirect
[ ] 12. Add middleware.ts for route protection
        - Unauthenticated → redirect to /auth/login
        - Authenticated on /auth/* → redirect to /dashboard

TYPES & CLIENTS
---------------
[ ] 13. Fill in types/index.ts
        - User, Language, Session, Attempt, Phrase, Feedback
[ ] 14. Initialize lib/anthropic.ts — export Anthropic client
[ ] 15. Initialize lib/elevenlabs.ts — export TTS + STT helpers
[ ] 16. Initialize lib/phonemes.ts — parse/compare phoneme arrays

API ROUTES
----------
[ ] 17. GET  /api/languages — return user's languages
[ ] 18. POST /api/languages — add a language for the user
[ ] 19. GET  /api/phrases?lang= — return practice phrases
[ ] 20. POST /api/tts — { text, language } → ElevenLabs audio
[ ] 21. POST /api/stt — audio blob → ElevenLabs transcript
[ ] 22. POST /api/analyze — { phraseText, transcript, language } → Claude score + phoneme feedback

COMPONENTS
----------
[ ] 23. LanguageCard — name, code, last practiced, streak; links to /practice/[lang]
[ ] 24. AddLanguageModal — language picker; calls POST /api/languages
[ ] 25. PhraseCard — phrase text, TTS play button, score slot
[ ] 26. RecordButton — MediaRecorder start/stop, emits audio blob
[ ] 27. WaveformVisualizer — Web Audio API canvas visualizer
[ ] 28. FeedbackPanel — score (0–100) + phoneme breakdown
[ ] 29. SessionChat — phrase → attempt → feedback conversational loop

PAGES
-----
[ ] 30. Landing page (app/page.tsx) — hero + CTAs to signup/login
[ ] 31. Dashboard (app/dashboard/page.tsx) — language cards + AddLanguageModal
[ ] 32. Practice page (app/practice/[lang]/page.tsx)
        - Fetch phrases → PhraseCard + RecordButton + WaveformVisualizer + FeedbackPanel
        - On recording: POST /api/stt → POST /api/analyze → save Attempt

PERSISTENCE & GAMIFICATION
---------------------------
[ ] 33. Save Session + Attempts to DB during practice
        - Create Session at start, save each Attempt, finalize on completion
[ ] 34. XP + streak logic — award XP per attempt, update UserProfile after session
[ ] 35. Achievements — define slugs (first_session, perfect_score, etc.), unlock after session
